/*
    * Logging wrapper fn
    * Logging decorator
    * Logging decorator with an argument
    * Read only decorator
    * Recreate coral declarative metrics?
*/
const Person = require('./person');

const p = new Person('alex', 30);
console.log(p.intro());